# Careem submission pack

Everything needed to satisfy the three requirements, with exact steps and
copy-paste text. Work through it top to bottom; it takes about 20 minutes.

---

## Requirement 1 — A public link, document, or screenshots

**What we're submitting:** a public GitHub repository, with a live dashboard
served from GitHub Pages and screenshots embedded in the README.

### Step 1.1 — Create the repository

1. Go to [github.com/new](https://github.com/new)
2. Repository name: `decision-brief-generator`
3. **Public**
4. Do **not** tick "Add a README" — this project already has one
5. Create repository

### Step 1.2 — Tell Git who you are (once per machine)

Skip only if you have used Git on this machine before. Without it, `git commit`
fails with *"Author identity unknown"*.

```powershell
git config --global user.name "Tulaib"
git config --global user.email "misbahamid30@gmail.com"
```

Use the same email as your GitHub account so the commits are attributed to you.

### Step 1.3 — Push the project

This block contains **no placeholders** — the repository URL is the real one.
Run it as written, from PowerShell, in the project folder.

```powershell
cd "$env:USERPROFILE\Desktop\Decision Brief Generator"

git init
git add .
git commit -m "Decision Brief Generator: raw data to costed executive decisions"
git branch -M main
git remote set-url origin https://github.com/MisbahHamid-30/decision-brief-generator.git
git push -u origin main
```

Three notes, each of which cost a failed attempt during setup:

- **`git remote set-url`, not `git remote add`.** `add` errors out if a remote
  already exists and leaves the old, wrong URL in place — so a second attempt
  looks like it worked and does not. `set-url` overwrites unconditionally.
- **Run each line separately.** Pasting two commands onto one line produces
  `fatal: too many arguments for a rename operation`.
- **Verify before pushing** if anything looks off:

  ```powershell
  git remote -v
  ```

  Both lines should read `https://github.com/MisbahHamid-30/decision-brief-generator.git`.
  `Repository not found` on push means this URL is wrong, not that authentication
  failed.

On `git push` a browser window opens asking you to sign in to GitHub. That is Git
Credential Manager; approve it and the push continues. GitHub no longer accepts
account passwords on the command line — if you are asked for one in the terminal
rather than a browser, generate a Personal Access Token at
**GitHub → Settings → Developer settings → Personal access tokens → Tokens
(classic)** with the `repo` scope, and paste that as the password.

If `git` is not recognised, install it from
[git-scm.com](https://git-scm.com/download/win) and reopen PowerShell.

`.gitignore` excludes both datasets and `node_modules`, so the push is small and
quick. `.gitattributes` normalises line endings — without it Git prints an
LF→CRLF warning for every file, which is harmless but noisy.

### Step 1.4 — Turn on the live dashboard

1. In the repo: **Settings → Pages**
2. Source: **Deploy from a branch**
3. Branch: **main**, folder: **/docs**
4. Save

After a minute or two the dashboard is live at:

```
https://misbahhamid-30.github.io/decision-brief-generator/
```

This is the strongest single artefact in the submission — an interactive
dashboard they can click through without installing anything.

### Step 1.5 — Fill in the links in the README

Both links are already filled in:

- Pages: `https://misbahhamid-30.github.io/decision-brief-generator/`
- Drive: `https://drive.google.com/drive/folders/1zZB-89sijqsABALomzSrZTa4O4vzpmNm?usp=sharing`

Push any outstanding edits:

```powershell
git add .
git commit -m "Add dataset link"
git push
```

---

## Requirement 2 — A public dataset link

The data is **self-created and synthetic**, which the brief explicitly permits.
Two things to submit: the dataset itself, and the generator that produced it.

### Step 2.1 — Upload to Google Drive

1. Open [drive.google.com](https://drive.google.com)
2. New → Folder → name it `Careem Decision Brief Generator — Datasets`
3. Create two subfolders inside it:
   - `careem_quik` — drag in all 9 CSVs from `data\careem_quik\`
     plus `data\DATA_DICTIONARY.md`
   - `careem_rides` — drag in all 6 CSVs from `data\careem_rides\`
     plus `data\DATA_DICTIONARY_RIDES.md`
4. Right-click the **top-level** folder → **Share** → General access →
   **Anyone with the link** → Viewer → **Copy link**

### Step 2.2 — Note for the form

> Both datasets are self-created synthetic data (permitted by the brief),
> generated by seeded scripts in the repo that reproduce them exactly.
>
> **Dataset 1 — supply chain:** 3.1M rows across 9 related tables modelling a
> UAE dark-store grocery network. Six deliberately planted signals.
>
> **Dataset 2 — marketplace:** 344k trips across 6 tables modelling UAE
> ride-hailing. Three planted signals. Built specifically to test whether the
> pipeline generalises beyond the domain it was written for — it did not, and
> `PORTABILITY.md` documents the six things that had to change.
>
> Both contain planted signals used as acceptance tests, so "the analysis found
> some problems" is a checkable claim rather than an assertion.

---

## Requirement 3 — 100-word summary

Copy this exactly. **98 words.**

---

I built a tool that turns raw operational data into an executive decision brief.
It quality-gates the data, refusing to run if integrity fails, then computes KPIs
and runs detectors. Every finding carries its numbers, method and confidence.
Findings link to root causes so the same money isn't counted twice; that removed
AED 781k of double counting. Recommendations net off the cost of acting, so one
rejects itself. Tested on two synthetic datasets — a supply chain and a
ride-hailing marketplace — with nine planted signals. All nine recovered,
including two misdiagnosis traps, verified by 36 independent checks.

---

### If they want something shorter

**50 words:**

> A tool that turns raw operational data into an executive decision brief. Every
> finding carries its evidence, method and confidence; findings link to root
> causes so the same money isn't counted twice. Recommendations net off the cost
> of acting — so one rejects itself. Verified by 26 independent checks.

**One line:**

> Raw operational data in, costed executive decisions out — with every claim
> traceable to a row, and recommendations honest enough to reject themselves.

---

## What to paste into the form

| Field | Value |
|---|---|
| Prototype link | `https://github.com/MisbahHamid-30/decision-brief-generator` |
| Live demo | `https://misbahhamid-30.github.io/decision-brief-generator/` |
| Dataset link | `https://drive.google.com/drive/folders/1zZB-89sijqsABALomzSrZTa4O4vzpmNm?usp=sharing` |
| Summary | the 96-word text above |

If the form takes only one link, submit the **GitHub repo** — the README leads
with the live dashboard and the screenshots, so everything else is one click
away.

---

## Pre-submission checklist

- [ ] Repo is **public** (not private)
- [ ] GitHub Pages is enabled and the dashboard actually loads
- [ ] Both README placeholders replaced with real URLs
- [ ] Drive folder set to "Anyone with the link" — **test it in a private
      browser window**, this is the most common failure
- [ ] `outputs/Decision-Brief.docx` and `.pptx` open correctly after download
      from GitHub
- [ ] The synthetic-data disclaimer is visible — it appears on the repo README,
      the brief cover, the deck cover, and the dashboard banner

---

## If they ask follow-up questions

Three things worth having ready, because they are the most likely probes.

**"How do you know the analysis is right?"** — `verify_outputs.py` imports none
of the pipeline's code. It recomputes every headline figure from the raw CSVs
with independent pandas. Verifying an analysis by calling the same engine only
proves the engine is deterministic. 26/26 checks pass.

**"What would you do differently with real data?"** — Two assumptions are soft
and flagged as such in the brief's appendix: the supplier premium for smaller
case packs is a placeholder until suppliers quote, and the 50% demand-migration
assumption behind the delisting recommendation is unverified. Both would need
real inputs before anyone acted. Customer lifetime value is absent entirely,
which almost certainly understates the cost of repeated service failure.

**"Does this generalise beyond supply chain?"** — Ingestion, quality gate, KPI
engine, ranking, recommendation economics and all three renderers are
domain-agnostic and driven by two config files. The eight detectors are not —
"fill rate" means nothing for marketing data. A new domain needs its own
detectors and its own acceptance test. That boundary is stated in the README
rather than glossed over.
